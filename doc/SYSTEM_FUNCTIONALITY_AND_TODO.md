
## АЦП и фильтрация

- **Текущая реализация**: SMA (скользящее среднее) в `adc.c` (stm_PPKY, MCU_TC, MCU_igniter_v03).
- **Требование**: добавить **параметр измерения СКО (стандартное отклонение)** по каждому каналу для валидации.
- **Результат**: флаг **достоверности** по каждому каналу (значение валидно / невалидно).
- **Местоположение**: `adc.c` во всех проектах с АЦП.

---

##  ППКУ: хранение и проверка настроек МКУ

- ППКУ хранит настройки всех МКУ.
- При запуске: считывает настройки (или контрольную сумму) и **сравнивает**.
- Если настройки **отличаются** — сообщить определённым **флагом**.
- Далее — функционал логирования и конфигурирования.

---

## Силовой ключ питания (BTS740S2)

- **Текущая реализация**: `PControl` в `stm_PPKY`, GPIO ST/OUT, без прерывания по статусу.
- **Сигнал ST (GPIO)**:
  - **ST = 1**: питание выключено, либо КЗ/неисправность.
  - **ST = 0**: питание включено и всё в норме.
- **Требование**:
  1. Настроить **прерывание на ST** (по фронту/уровню при переходе в 1).
  2. При ST = 1 при включённом питании — **сразу отключать** выход.
  3. Пытаться **повторно включить** через N мс (пока 2), до M попыток (пока 1 с).
  4. После неудачи — выставить **флаг ошибки питания**.
- **Местоположение**: `stm_PPKY/Core` (power_control, main, прерывания).

---

## Настройки ППКУ

- **Выбор питания**: список 0, 1, 2.
- **Наличие БРП**: 0 или 1.
- **Местоположение**: `PPKYCfg` в `device_config.h`, конфигурация ППКУ.

---

### Статусные лампочки

| # | Константа      | Описание                          |
|---|----------------|-----------------------------------|
| 0 | LED_POWER      | Лампочка питания                  |
| 1 | LED_NORM       | НОРМА                             |
| 2 | LED_START      | ПУСК                              |
| 3 | LED_STOP       | ОСТ ПУСК                          |
| 4 | LED_ERR        | Неисправность                     |
| 5 | LED_FIRE       | Пожар                             |
| 6 | LED_AUTO_OFF   | Режим тушения ручной (автоматика выкл.) |

###  Подсветка кнопок

- **Большие**: LED_BUT_START_ALL (7), LED_BUT_STOP (8), LED_BUT_START_SP (9) 
- **Подписи**: LED_STR_START_ALL (10), LED_STR_STOP (11), LED_STR_START_SP (12).
- **Мелкие**: LED_BUT_ENTER_UP (13), LED_BUT_ESC_DW (14) 


## Входы питания ППКУ

- Два входа питания, каждый измеряется.
- При пропаже по одному — логика LED_POWER (см. п. 8.5).



# TODO: План изменений системы

## Приоритет 1 — Backend и протокол

| # | Задача | Где | Статус |
|---|--------|-----|--------|

## Приоритет 2 — Виртуальные устройства

| # | Задача | Где | Статус |
|---|--------|-----|--------|
| 8 | ДПТ: режим определения сопротивления, пороги состояний, управление питанием и фильтрация уровней (переключение на термопару при КЗ уже реализовано) | device_dpt.cpp, MCU_TC | ◐ |

## Приоритет 3 — АЦП и валидация

| # | Задача | Где | Статус |
|---|--------|-----|--------|
| 11 | Добавить расчёт СКО по каждому каналу АЦП | adc.c (все проекты) | ⬜ |
| 12 | Добавить флаг достоверности по каждому каналу | adc.c, app | ⬜ |

## Приоритет 4 — Режимы и конфигурация

| # | Задача | Где | Статус |
|---|--------|-----|--------|
| 15 | Параметры: выбор питания (0,1,2), наличие БРП (0,1) | PPKYCfg | ⬜ |


TODO неотсортированный::
задержка soft reset, иначе не ренранслирует команды
отключение каналов на МКУ настройкой
внешний CAN протоколы 1939 и 1979, выбор слота из резерва настройкой в ППКУ
автономный режим содержит автономную задержку (всегда 60с) и задержку модуля.

#чтение исторических данных:
 Устройства по команде от мастера начинают передачу интервала данных из своего буфера. Хардкомим 5минут данных - но это сделать легко меняемым.
 Каналы АЦП 10-12 бит, поэтому будем слать данные по 2 байта. в пакет можем уместить 2 значения (4 байт + 1 байт команда + 1 байт номер пакета циклический + 1 байт тип данных ????)
 Одновременно читать все сразу не выйдет, поэтому сначала шлём общую команду - запомнить масссив, т.е перестать переписывать чиклические данные.
 Устройство по команде запросу начинает передавать свои данны, начиная с самых старых и заканчивая самыми свежими. По сле передачи всего обязательно пакет вадидации, что всё отправили.
 Так мастер поймёт что нужно запрашивать новый слейв.
 Предпологается что отвечать данные будут вирт устрйоства, чтобы не запутаться в потоке. 
 Поэтому буфер выделяемый в МКУ должен управляться с вирт устрйоства через указатели и функции. 
 Само вирт устройство лишь знает указатель и размер буфера (скорее всего прямо в кол-ве значений а не байтах).
 


#в верхнем баре должен быть значок "!" в треугольнике, означающий наличие какой-либо неисправности

#меню диспетчер устройств:
- верхее поле номер зоны.
- по центру МКУ - справа может быть символ ! если там ошибка серийника\позиции
- в нижней строке новое мку, так же может быть с !
- стрелками вверх\вниз меняем МКУ. 
при смене верхяя строка указывает зону МКУ.
нижняя показывает следующую мку.
- долгое нажатие enter - применить все изменения, которые соответствуют типу
- по нажатию enter открывается меню мку, где серийник, зона. код ошибки если он есть. 
по enter при ошибке - всплывашка "применить новую настройку" - enter сохранить и выход в меню мку, esc - выход назад в меню мку 
или при наличии ошибки типов - "ошибка монтажа" - esc - выход назад в меню мку 

# отдельное меню - список всех неисправностей 

# отдельное меню - список всех устрйоств на шине


#promt