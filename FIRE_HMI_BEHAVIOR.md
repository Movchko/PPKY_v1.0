# Пожар на ППКУ: кнопки, индикаторы, экран (актуальная логика)

Документ описывает поведение после правки **«ОСТАНОВ ПУСКА»** и сопутствующих правил. Источник истины в коде: `Core/Src/fire.c`, TouchGFX `mainscreenView`, контейнер бегущей строки.

---

## Состояния и слоты

- **Слот пожара** — одна зона с активным пожаром в текущем цикле: хранится задержка до возможной авто-фазы 2, флаг «фаза 2 уже отправлена».
- **FIRE_STATE_IDLE** — нет активных слотов.
- **WAIT_AUTO / WAIT_MANUAL** — есть слоты, у кого-то ещё не отправлена фаза 2 (ожидание таймера или ручного пуска).
- **EXTINGUISHING** — по всем слотам фаза 2 уже ушла на МКУ; слоты при этом **остаются** (зоны «с пожаром» для UI), пока цикл не сброшен командой с шины.

Режим **Авто/Ручной** задаётся полем `PPKYConfig.fire_mode` (`2` = ручной, индикатор «Авто откл»).

---

## Экран (главный)

- **Число (таймер)** — минимальный оставшийся отсчёт **задержки зоны** до авто-фазы 2 среди слотов, где фаза 2 ещё не ушла. Пусто / 0, если отсчёт не показывается.
- После **«ОСТАНОВ ПУСКА»** включён флаг **`zone_countdown_stopped`**: авто-отсчёт **не считается**, на экране **таймер не показывается** (кроме отдельного случая удержания «ПУСК ОБЩИЙ», см. ниже).
- **Имена зон** — бегущая строка: по очереди одно имя, длинные имена прокручиваются; пауза 3 с между зонами (логика в TouchGFX).

---

## Кнопка «ОСТАНОВ ПУСКА» (BUT_STOP)

Действия:

1. Перевод в **ручной режим**: `fire_mode = 2`.
2. Устанавливается **`zone_countdown_stopped = 1`** — останавливаются **авто-таймеры зон** и **их отображение** на экране.
3. На шину уходит **StopExtinguishment** ко **всем МКУ** (как и раньше по составу типов).
4. **Слоты пожара не сбрасываются**, сценарий **не переходит в IDLE**.
5. Пищалка переводится в **дежурный режим** (короткий писк раз в N секунд), т.к. событие пожара считается обработанным.
5. Дальнейшая индикация: **LED ПОЖАР мигает**, на экране по-прежнему **список зон с пожаром** (имена), состояние FSM синхронизируется со слотами (при необходимости — `EXTINGUISHING`, если фаза 2 уже была везде).

Сброс **`zone_countdown_stopped`** выполняется только при **перезапуске ППКУ** (инициализация `Fire_Init`).

---

## Команда StopExtinguishment с CAN (не кнопка)

- Обрабатывается как **`FIRE_EVENT_STOP_EXT`**: отправляется стоп на МКУ и в ППКУ отключается авто-отсчёт (`zone_countdown_stopped = 1`).
- **Состояние пожара не сбрасывается**, **слоты не очищаются**, перехода в **IDLE** нет.
- Пищалка/индикация пожара сохраняются, список зон на экране остаётся.

Это не «полный сброс», а остановка таймеров/автопуска без очистки цикла пожара.

---

## Кнопка «ПУСК СП» (BUT_FIRE)

- Имеет смысл **только если есть активные слоты пожара** и **есть хотя бы одна зона без отправленной фазы 2**.
- Иначе нажатие **ничего не делает** (нет пожара / нечего запускать по фазе 2).
- Работает в состояниях **WAIT_AUTO**, **WAIT_MANUAL** и **EXTINGUISHING** (если ещё есть ожидающие фазу 2 зоны — на практике в основном WAIT_*).

Действие: отправка **фазы 2 сразу для всех** зон, где она ещё не отправлена.

---

## Кнопка «ПУСК ОБЩИЙ» (BUT_FORCE, удержание 3 с)

- Удержание 3 с: отдельный **краткий отсчёт на экране** (3…1) — **показывается даже при** `zone_countdown_stopped` (это не таймер зоны).
- После удержания: **то же условие**, что у ПУСК СП — нужны **слоты** и **ожидающие фазу 2**; иначе команды фазы 2 не уходят.
- Действие: **фаза 2** на **все** зоны, где она ещё не отправлена.

---

## Индикаторы LED (кратко)

- **LED_AUTO_OFF** (или аналог «авто откл»): горит в **ручном** режиме (`fire_mode == 2`).
- **ПУСК СП / подпись СП, СТОП / подпись СТОП**: подсвечены в режиме ожидания, пока **есть зоны без фазы 2** (`Fire_CountPendingPhase2() > 0`), плюс логика удержания «ПУСК ОБЩИЙ».
- Когда **все** слоты получили фазу 2: стиль как «тушение запущено» — **ПУСК СП/СТОП** гаснут, **ПУСК ОБЩИЙ** яркий, **LED START** горит (как было в коде).
- **LED ПОЖАР**: мигает (~0,5 Гц), пока состояние **не IDLE** (пожар по слотам ещё «активен» для индикации).

---

## Пищалка (пожар)

- При новом `StatusFire` — **сигнальный режим**: непрерывный писк.
- Сигнальный режим держится до обработки пожара:
  - нажата `ОСТАНОВ ПУСКА`,
  - нажата кнопка `ПУСК СП` / `ПУСК ОБЩИЙ`,
  - либо сработал авто-таймер зоны и отправилась фаза 2.
- После обработки — **дежурный режим**: короткий писк раз в `FIRE_DUTY_BEEP_PERIOD_MS` (сейчас 5 с).
- При следующем новом пожаре снова включается сигнальный непрерывный режим.

---

## Приём «пожар» по CAN (StatusFire)

- Новая зона: слот, **фаза 1** на igniter, ответ `ReplyStatusFire`.
- Повтор по той же зоне в одном цикле **игнорируется** (слот не обновляется).

---

## Заметки для доработки ТЗ

